.section
  .wrap
    .content-artist-info
      h1 = @resource.title
      hr
      .col-1
        .event-info
          - unless @resource.artist_address.empty?
            span.location
              strong Location:
              = @resource.artist_address
          - unless @resource.awards.empty?
            span.awards
              strong Awards:
              == @resource.awards(&:to_html).join(', ')

      .col-2
        == content @resource.body
